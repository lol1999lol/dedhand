# Changelog

## 2.2.0

- `guide` / `quickstart` next-step helper; `init` alias; easier install-service
- Env setup (`DEDHAND_NAME`, `DEDHAND_PASS`, `DEDHAND_HOURS`, Telegram env)
- Author **lol1999lol** in banner, CLI, docs, and package metadata
- Clearer README (EN + نصب سریع)

## 2.1.0

- Vault targets for zip archives and databases (sqlite snapshot, sql dumps)
- `add` accepts multiple paths; doctor notes live DBs and large files
- Archives stored without recompression inside the drop zip

## 2.0.0

- Elite CLI: boxed banner, `which`, `export`, `status --json`, `passwd`
- systemd uninstall, Gotify channel, SHA-256 vault seals
- Integrity tick (missing path + hash drift), stricter fire completion
- GitHub CI on Node 20 and 22, changelog, richer doctor

## 1.1.0

- GitHub-ready layout, MIT, encrypted state backup

## 1.0.0

- Headless daemon, Telegram inbox, multi-channel publish
